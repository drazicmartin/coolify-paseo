version: "3.8"

services:
  paseo:
    build: .
    volumes:
      - paseo_workspace:/workspace
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

volumes:
  paseo_workspace: